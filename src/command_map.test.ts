import {describe, expect, test} from "vitest";

describe.each([
  {
    input: "",
    expected: "",
  },
])("fetchLocations($input)", ({ input, expected }) => {
  test(`Expected: ${expected}`, () => {
    let actual = 0;



  });
});
